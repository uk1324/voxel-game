#pragma once

// Maybe later orginize it so there is a templated function that takes a function to draw the scene.
// And maybe split the shaders for calculating different things like shadows into different files.
// Then the functions wouldn't use any uniforms and just take the inputs directly.

class ShadowMapping
{
private:

};